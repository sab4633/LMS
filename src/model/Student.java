package model;

public class Student extends User{

    //fields
    /**\NOTE to self the grades/assignments can probably be accessed from all the class sections
     * list of classes
     * list of grades
     * list of assignments
     *
     *
     */

    //constructors
    public Student(int userID) {
        super(userID);
    }
    //methods
}
